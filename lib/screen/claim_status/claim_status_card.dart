import 'package:flutter/material.dart';

class ClaimStatusCard extends StatelessWidget {
  const ClaimStatusCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
