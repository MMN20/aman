import 'package:flutter/material.dart';

class ClaimsStatusPage extends StatelessWidget {
  const ClaimsStatusPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("حالة المطالبة"),
      ),
    );
  }
}
